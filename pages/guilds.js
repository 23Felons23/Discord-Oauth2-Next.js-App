import SecureTemplate from '../public/secure-template';
function About() {
    return (
        <div>
            <p>This is the guilds page</p>
        </div>
    );
}

export default SecureTemplate(About)